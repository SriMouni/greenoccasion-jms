# Shell scripts and unit files run on Linux — force LF so they work after a
# Windows checkout/commit (CRLF breaks the bash shebang and heredocs).
*.sh             text eol=lf
*.service        text eol=lf
deploy/Caddyfile text eol=lf
